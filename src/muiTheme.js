import { createTheme } from "@mui/material";

const muiTheme = createTheme({
  palette: {
    primary: {
      main: '#63C573',
    },
    secondary: {
      main: '#ACCE42',
    },
  },
})

export default muiTheme;
